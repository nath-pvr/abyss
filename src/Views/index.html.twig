{% extends "template.html.twig" %}

{% block navigation %}
        <nav>
            <ul>
                {# TODO Vérifier si la super globale $_SESSION['auth'] est initialisée et si oui, rajouter mettre un moyen de se déconnecter #}
                <li><a href="{{ baseUrl }}/inscription">Inscription</a></li>
                <li><a href="{{ baseUrl }}/connexion">Se connecter</a></li>
                <li>
                    <input type="text" name="search" placeholder="Rechercher...">
                </li>
            </ul>
        </nav>
{% endblock %}

{% block body %}
    {% for movie in movies %}
        {{ dump(movie) }}
    {% endfor %}
{% endblock %}
